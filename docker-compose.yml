---
streammachine-master:
  build: master/
  ports:
    - "8000-8010:8000-8010"
  links:
    - redis
streammachine-slave:
  build: slave/
  ports:
    - "80:80"
  links:
    - redis
    - streammachine-master
redis:
  image: redis
